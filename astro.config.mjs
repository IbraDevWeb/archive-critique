import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Configuration de base : génération de site statique (SSG)
  // Idéal pour la vitesse et le SEO de tes fiches.
});